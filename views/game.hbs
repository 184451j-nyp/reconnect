<div class="row mt-4">
    <div class="col text-center">
        <p>Room code: {{code}}</p>
    </div>
</div>
<div class="row mt-675">
    <div id="panel" class="col-6 mx-auto text-center question">
        <p>{{question}}</p>
        <span>
            {{#if shuffle}}
            <input type="button" id="btnShuffle" class="btn btn-link mr-2" value="Next Question">
            {{/if}}
            {{#if deeper}}
            <input type="button" id="btnDeeper" class="btn btn-link" value="Go Deeper">
            {{/if}}
        </span>
        <br>
        <a href="/endgame">End game.</a>
    </div>
</div>